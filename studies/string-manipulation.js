/**With operators
*with String methods 
* 
* String Manipulation
* 
* Strings are accessed in indices ( start with 0)like arrays
* var string = 'Dog'
* return string[2] <--- returns g
* 'string'.toUpperCase
* 'string'.toLowerCase
* 'string'.length <--- gives length of string
.join(' ') <--- Joins at what is in the parentheses
.Split(' ') <--- Splits string at what is in the parenthesis, splits string into array
concatinate : add together
'Dot' + " " + 'Behrens'